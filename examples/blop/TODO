[] Schedule
[] ScheduleService

Observation Mode:
    [] Include special tiles
        [] Stairs
    [] Include Blops
    [] Limit to FOV -or- Use Memory -or- Everything

Path Following:
    [] Show path to mouse
    [] Allow Click to make Hero follow path
    [] Also Enter?
    [] Stop following automatically
        - Pickup something
        - Take damage
        - See Blop - only new?

Blops:
    [] Add blops
    [] Blop AI
    [] Combat

MultiWorld:
    [] Allow up+down stairs

Effects:
    [] Add more Effects

Triggers:
    [] Add more Triggers

FOV:
    [X] Keep FOV for Hero
    [X] Recalc on every move
    [X] Recalc on teleport
    [] Keep memory of map as it is exposed
        [] Only items and tiles
        X Not Blops (Too hard because of dissappear, reappear logic)
    [] Use Memory for Draw of revealed
    [] FocusHelper needs to be FOV aware
        * Only keep tiles+items that are revealed
        * Only keep actors that are visible
        * Make sure FOV is updated - not as easy as check in reset

Pickup:
    [X] Need to update FocusHelper
        * Remove picked up entity
        * store.notify?