[] Teleport flash
[] Show items in Sidebar (above tile)
[] FOV
[] Schedule
[] ScheduleService

Observation Mode:
    [] Hit 'Tab' to go into Observation mode
    [] Tab moves to next entity (non-tile)
        - How to do this?
    [] Arrows move focus
    [] Mouse move/click moves focus + leaves mode
    [] 'Escape' leaves observation mode

Path Following:
    [] Show path to mouse
    [] Allow Click to make Hero follow path
    [] Also Enter?
    [] Stop following automatically
        - Pickup something
        - Take damage
        - See Blop - only new?

Blops:
    [] Add blops
    [] Blop AI
    [] Combat

MultiWorld:
    [] Allow up+down stairs

Effects:
    [] Add more Effects

Triggers:
    [] Add more Triggers
